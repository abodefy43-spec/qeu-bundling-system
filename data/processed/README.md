# data/processed

Active Phase 0 runtime output lives here.

Canonical subdirectories:

- `features/`
- `artifacts/`
- `reports/`
- `runs/`

The bootstrap pipeline writes its current status to `reports/phase0_status.json`.
