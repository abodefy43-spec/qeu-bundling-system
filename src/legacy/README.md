# src/legacy

The old `qeu_bundling` package is preserved here for reference while the active repository moves forward with the cleaned `src/api`, `src/data`, `src/engines`, `src/features`, `src/pipelines`, `src/shared`, and `src/utils` layout.
